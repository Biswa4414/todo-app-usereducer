import React from "react";
import TodoApp from "../src/Components/TodoApp";

const App = () => {
  // Rendering the TodoApp component
  return (
    <div>
      <TodoApp />
    </div>
  );
};

export default App;
